{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    {% for article in articles %}
        <div>
            <h2>{{ article.title }}</h2>
            <p>{{ article.content }}</p>
        </div>
    {% else %}
        Aucun article trouvé.
    {% endfor %}
    <a href="{{ path('category', { 'categoryName' : 'Javascript'}) }}">
        Return Javascrip category.
    </a>
    <a href="{{ path('app_index') }}">
        Back to homepage.
    </a>
{% endblock %}